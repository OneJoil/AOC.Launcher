<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:Gml.Launcher.Views.Components">
    <Design.PreviewWith>
        <Grid Width="800"
              Height="600"
              Background="{StaticResource BackgroundColor}"
              ColumnDefinitions="Auto, *">

            <controls:SidebarComponent Grid.Column="0" />

        </Grid>
    </Design.PreviewWith>

    <Style Selector="controls|SidebarComponent">
        <Setter Property="Template">
            <ControlTemplate>

                <Border Padding="0, 20"
                        Background="{StaticResource BackgroundColor}">
                    <Grid RowDefinitions="Auto, 0, *, 80, Auto"
                          Width="80">

                        <StackPanel Grid.Row="2"
                                    Spacing="10"
                                    VerticalAlignment="Top">

                            <!-- <controls:GmlButton Classes="Icon Secondary" -->
                            <!--                     Command="{TemplateBinding HomeCommand}" -->
                            <!--                     Width="60" -->
                            <!--                     Height="60" -->
                            <!--                     IconPath="/Assets/Images/home.svg" -->
                            <!--                     IconSize="22" /> -->

                            <controls:GmlButton Classes="Icon Secondary"
                                                Command="{TemplateBinding ProfileCommand}"
                                                Width="60"
                                                Height="60"
                                                IconPath="/Assets/Images/profile.svg"
                                                IconSize="24" />

                            <controls:ServerListComponent DataContext="{TemplateBinding ListViewModel}" Padding="15"/>

                        </StackPanel>

                        <StackPanel Grid.Row="4"
                                    Spacing="10"
                                    VerticalAlignment="Bottom">

                            <controls:GmlButton
                                Classes="Icon Secondary"
                                Command="{TemplateBinding InfoCommand}"
                                Width="60"
                                Height="60"
                                IconPath="/Assets/Images/info.svg"
                                IconSize="25"/>

                            <controls:GmlButton
                                Classes="Icon Secondary"
                                Command="{TemplateBinding LogoutCommand}"
                                Width="60"
                                Height="60"
                                IconPath="/Assets/Images/logout.svg"
                                IconSize="20" />

                        </StackPanel>

                    </Grid>
                </Border>

            </ControlTemplate>
        </Setter>
    </Style>
</Styles>
